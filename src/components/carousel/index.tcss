.wrap-carousel {
	position: relative;
}
.carousel-item{
	height: 100vw;
}
.indicator-number{
	position: absolute;
	bottom: 16px;
	right: 16px;
	background: rgba(0, 0, 0, 0.4);
	border-radius: 16px;
	padding: 6px 12px;
	color: #fff;
}